# Spring Framework 스터디 리뷰 지침

## 프로젝트 목적

이 저장소는 Spring 공식 문서를 기반으로 주차별 과제를 수행하는 스터디 저장소다. 주된 작업은 구현이 해당 주차의 GitHub Issue 요구사항을 충족하는지 PR 단위로 검토하고, PR에 남기기 좋은 코멘트와 피드백 후보를 사용자에게 제공하는 것이다.

## 리뷰 대상 제한

- 현재 GitHub 인증 사용자에게 리뷰가 요청된 열린 PR만 리뷰한다.
- `gh api user --jq .login`으로 현재 사용자를 확인하고, PR의 `reviewRequests`에 해당 사용자가 있는지 검증한다.
- 사용자가 할당된 모든 PR 리뷰를 요청하면 `review-requested:@me`인 열린 PR 전체를 빠짐없이 처리한다.
- 리뷰 요청이 없거나 이미 해제된 PR은 사용자가 번호를 언급해도 리뷰하지 말고, 대상이 아닌 이유만 알린다.
- Issue의 `PR Reviewer`와 `다음 주차 발표자` 정보는 참고용이다. 실제 리뷰 가능 여부는 PR의 GitHub review request만으로 판단한다.
- 리뷰는 읽기 전용이다. 리뷰 리포트와 이 지침 문서 외의 저장소 파일, commit, branch, PR metadata를 변경하거나 push하지 않는다. 빌드가 만드는 ignored 산출물만 허용한다.
- 사용자의 명시적 재확인 없이 PR에 review, comment, approve, request changes를 직접 등록하지 않는다.

## 리뷰 기준

1. PR 본문의 같은 저장소 `#이슈번호`를 확인해 실제 주차 Issue인지 검증한다. 참조가 없으면 브랜치명과 PR 제목으로 주차를 추정하되, Issue 미첨부 자체도 피드백하고 추정 사실을 밝힌다. 후보가 여러 개라 모호하면 임의로 고르지 않는다.
2. Issue에서 현재 주차의 `과제`와 제출 조건만 체크리스트로 추출한다. `다음 주차 스터디`와 발표자 과제는 현재 PR의 구현 요구사항으로 취급하지 않는다.
3. PR 전체 diff를 읽고 다음 순서로 검토한다.
   - 과제 항목, 패키지명, 클래스·메서드 시그니처, 상태 변화, 로그와 첨부 이미지 충족 여부. 첨부 이미지는 존재만 확인하지 말고 직접 열어 요구한 값과 실행 결과가 보이는지 확인한다.
   - 코드가 실제로 실행되는지와 Spring 컨테이너 수명주기·이벤트·DI 등 관련 개념을 올바르게 사용했는지
   - 예외, 상태 공유, 순서 의존성처럼 실행 결과를 깨뜨릴 수 있는 문제
   - 변경된 참여자 프로젝트의 빌드·테스트 결과
   - 학습을 돕는 설명, 질문, 잘한 점
4. 현재 과제와 관련된 Spring 공식 Reference Documentation/Javadoc을 우선 근거로 사용한다. `다음 주차 스터디`에만 있는 링크를 현재 과제의 근거로 오인하지 않는다. 버전별 동작이 중요하면 해당 프로젝트의 `build.gradle` 또는 `pom.xml` 버전과 맞는 공식 문서로 검증하며, 블로그만으로 오류를 단정하지 않는다.
5. `baseRefOid`와 `headRefOid`를 기록하고 PR head SHA의 격리된 임시 worktree에서 검증한다. 현재 worktree를 checkout하거나 사용자 변경을 건드리지 않는다. 참여자 디렉터리에서는 실행 비트 유무와 관계없이 `bash ./gradlew test`를 사용하고, 테스트가 0건이면 그 사실을 밝힌다. 과제가 실행 결과를 요구하면 관련 main/profile도 가능한 범위에서 실행한다. 루트 코드가 변경된 경우에만 루트의 `mvn test`를 확인한다.
6. GitHub base와 head 사이의 전체 diff는 모두 확인하되, 과제 충족 판정은 현재 주차에 해당하는 변경 범위를 분리해 수행하고 그 범위를 밝힌다. 잘못된 base branch나 불필요한 누적 변경이 리뷰를 방해하면 별도 피드백한다.
7. 실행하지 못한 검증과 이미지에서 판독할 수 없는 내용은 추측하지 말고 사유와 `확인 불가` 범위를 적는다.

## Codex·Claude 병렬 리뷰

- PR마다 `orchestration` skill을 사용해 Orca runtime에서 Codex와 Claude의 독립적인 review-only task를 각각 생성하고 `dispatch --inject`로 전달한다. 일반 sub-agent 호출로 대체하지 않는다.
- 시작 전에 `orca status --json`으로 runtime을 확인하고, 각 agent를 현재 worktree의 별도 터미널에서 실행한다. worker는 checkout이나 빌드를 하지 않는다. 작업·dispatch 생성 여부는 `task-list`와 `dispatch-show`로 검증한다.
- 두 task spec에 파일·commit·branch·GitHub 쓰기 금지와 결과만 반환하라는 문구를 명시한다.
- 두 agent에게 동일한 PR URL, head SHA, 연관 Issue, 과제 판정 범위, 전체 diff, 프로젝트 버전, 빌드·실행 결과를 제공한다. 각 agent는 전체 요구사항을 독립 검토하고 최소한 다음을 반환한다.
  - Issue 항목별 충족 여부
  - 재현 가능하고 실행에 영향을 주는 문제
  - 필요한 경우 공식 문서 근거
  - 가능한 경우 head SHA 기준의 정확한 파일과 diff 라인. 코드 위치가 없는 문제는 PR 본문, Issue 항목 또는 검증 명령을 위치로 사용한다.
  - 빌드·실행 검증과 필요한 질문·제안·칭찬
  - PR에 복사할 수 있는 한국어 코멘트 초안
- 두 `worker_done`을 기다린 뒤 결과를 원본 diff와 공식 문서로 재검증한다. 중복은 합치고, 근거 없는 의견은 제외하며, 충돌하는 의견은 어느 쪽이 맞는지 확인한 후 하나의 결론으로 정리한다.
- 최종 응답 직전에 `reviewRequests`를 다시 확인한다. 요청이 해제됐거나 조회할 수 없으면 코멘트 후보를 제공하지 말고 상태만 알린다.
- Orca orchestration을 사용할 수 없으면 다른 방식으로 두 agent를 사용했다고 가장하지 말고, 제약과 완료하지 못한 범위를 사용자에게 알린다.

## 최종 응답 형식

- **과제 체크리스트**: 각 항목을 `충족`, `부분 충족`, `미충족`, `확인 불가`로 표시
- **PR 코멘트 후보**: 중요도순으로 위치, 근거, 복사 가능한 한국어 코멘트 문구 제공
  - `[필수]`: 과제 미충족, 잘못된 동작, 빌드 실패
  - `[질문]`: 의도가 불명확해 확인이 필요한 내용
  - `[제안]`: 과제 통과와 무관한 개선
  - `[칭찬]`: 구체적으로 잘 구현한 내용
- **검증 결과**: 실행한 명령과 성공·실패·미실행 사유
- 지적할 문제가 없으면 억지 코멘트를 만들지 말고, blocking issue가 없다고 명시한 뒤 확인한 범위와 남은 위험만 적는다.

## 리뷰 리포트 저장

- 완료한 리뷰는 저장소의 `review/week{Issue 주차}/{PR 작업자}.md`에 기록한다. 작업자 이름은 PR 브랜치의 주차 뒤 식별자(예: `week8/sangjin`이면 `review/week8/sangjin.md`)를 우선 사용한다.
- 같은 작업자가 같은 주차에 PR을 여러 개 만들었으면 하나의 파일 안에 `## PR #{번호} — {제목}` 섹션을 PR별로 만들고 기존 섹션을 덮어쓰지 않는다.
- 각 PR 섹션에는 PR·Issue 링크, head SHA, 과제 체크리스트, 코멘트 후보, 검증 결과, Codex·Claude 취합 결론을 포함한다.
- 각 PR 섹션의 마지막 줄에는 필수 수정 사항이 없으면 `최종 상태: Approve`, 있으면 `최종 상태: Change Request`를 기록한다.
